---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: miniflux
  namespace: selfhosted
spec:
  secretStoreRef:
    kind: ClusterSecretStore
    name: onepassword-connect
  target:
    name: miniflux-secret
    creationPolicy: Owner
    template:
      engineVersion: v2
      data:
        OAUTH2_CLIENT_ID: "{{ .MINIFLUX_CLIENT_ID }}"
        OAUTH2_CLIENT_SECRET: "{{ .MINIFLUX_CLIENT_SECRET }}"
        DATABASE_URL: >-
          password={{ .db_password }} dbname={{ .db_dbname }} host={{ index . "db_pgbouncer-host" }}.cluster.local sslmode=disable user={{ .db_user }}
  dataFrom:
    - extract:
        key: Miniflux
      rewrite:
        - regexp:
            source: "(.*)"
            target: "MINIFLUX_$1"
    - extract:
        key: postgres-pguser-miniflux
      rewrite:
        - regexp:
            source: "(.*)"
            target: "db_$1"
      sourceRef:
        storeRef:
          kind: ClusterSecretStore
          name: crunchy-pgo-secrets
